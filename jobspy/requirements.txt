# JobSpy is vendored locally at jobspy/jobspy/ (not installed from PyPI) so we can add
# the custom Seek scraper. These are JobSpy's own runtime deps, pinned to match the
# vendored source (python-jobspy 1.1.82 pyproject.toml [tool.poetry.dependencies]).
requests>=2.31.0,<3.0.0
beautifulsoup4>=4.12.2,<5.0.0
pandas>=2.1.0,<3.0.0
numpy==1.26.3
pydantic>=2.3.0,<3.0.0
tls-client>=1.0.1,<2.0.0
markdownify>=0.13.1,<0.14.0
regex>=2024.4.28
